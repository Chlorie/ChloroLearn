#include "loss.h"

namespace chloro::operators
{
    //Operand categorical_cross_entropy(Operand result, Operand target)
    //{
    //    Operator op(
    //        [](InParams params)
    //        {

    //        },
    //        [](InParam gradient, InParams params)
    //        {

    //        }, { 1 });
    //    return Operand::join(std::move(op), { std::move(result), std::move(target) });
    //}
}
